name: .NET

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Setup .NET
      uses: actions/setup-dotnet@v2
      with:
        dotnet-version: 5.0.x
    - name: Restore dependencies
      run: dotnet restore
    - name: Build
      run: dotnet build --no-restore
    - name: Publish
      run: dotnet publish ${{ github.workspace}}\AnimalAdoption.Web.Portal\AnimalAdoption.Web.Portal.csproj -c Release -o release — nologo
    - name: Upload a Build Artifact
      uses: actions/upload-artifact@v3.1.0
      with:
        path: ${{ github.workspace}}\AnimalAdoption.Web.Portal\elease\
